


copy_linked_presentations <- function(presentation, spreadsheet, presentation_name, spreadsheet_name) {
  # Validate presentation and spreadsheet
  # Copy presentation
  # Copy spreadsheet
  # Unlink a chart, then re-link it to the new version
  # Use the mapping to un/re-link them using the name of the sheet
}

create_linked_chart_mapping <- function(presentation, spreadsheet) {
  # Take the presentation, find all linked charts, and get all the chart_id's of those linked charts, in order

  #Using the vector of chart_id's, get a vector of sheet_id's

  # Using the vector of sheet_id's, get a vector of sheet names

  # Return the vector of sheet names which will serve as a mapping.
}