# Get authentication token

Gets a Oauth Token. If it is not currently set, it called r2slides_auth
for authentication.

## Usage

``` r
r2slides_token()
```

## Value

An authentication configuration or \`NULL\` if authentication is not
active.
