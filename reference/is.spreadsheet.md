# Check if an object is a spreadsheet

Check if an object is a spreadsheet

## Usage

``` r
is.spreadsheet(x)
```

## Arguments

- x:

  An object to check

## Value

`TRUE` if `x` is a `spreadsheet`, `FALSE` otherwise
