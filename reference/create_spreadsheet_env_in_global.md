# Create spreadsheet environment

Create spreadsheet environment

## Usage

``` r
create_spreadsheet_env_in_global()
```

## Value

Creates a \`google_spreadsheet\` environment in the global environment
if one does not already exist.
