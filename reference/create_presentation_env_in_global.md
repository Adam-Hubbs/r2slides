# Create presentation environment

Create presentation environment

## Usage

``` r
create_presentation_env_in_global()
```

## Value

Creates a \`google_presentation\` environment in the global environment
if one does not already exist.
