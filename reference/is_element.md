# Test whether an object is an element

Test whether an object is an element

## Usage

``` r
is_element(x)
```

## Arguments

- x:

  An object to test

## Value

`TRUE` if `x` inherits from `element`, `FALSE` otherwise.
