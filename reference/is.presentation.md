# is.presentation

Check if an object is a presentation

## Usage

``` r
is.presentation(x)
```

## Arguments

- x:

  An object to check

## Value

\`TRUE\` if \`x\` is a \`presentation\`, \`FALSE\` otherwise
