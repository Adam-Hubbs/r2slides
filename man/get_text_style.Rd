% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_generics.R
\name{get_text_style}
\alias{get_text_style}
\title{Get the text style of an element}
\usage{
get_text_style(x, ...)
}
\arguments{
\item{x}{A \code{text_element} object.}
}
\value{
A \code{text_style} object (uniform case) or a \code{style_rule} object
(mixed-style case). Either can be passed directly to \code{add_text(text_style = ...)} to reproduce the styling on a new element.
}
\description{
Inspects every text run in the element and returns a styling object that can
be passed directly to \code{add_text()} via its \code{text_style} argument:
\itemize{
\item \strong{Uniform style} (all runs share identical character-level styles):
returns a single \code{text_style} object.
\item \strong{Mixed styles} (runs differ in at least one style property): returns a
\code{style_rule} with one \code{func}-type selector per run. Each selector function
returns \code{c(start, end)} 1-based inclusive character indices for that run,
matching the convention expected by \code{create_styling_request()}.
}

Note: only character-level styles (bold, italic, font size, color, etc.) are
captured. Paragraph-level styles (alignment, line spacing, indentation) live
on \code{paragraphMarker} entries, which \code{text_runs()} skips, so they will not be
reflected in the returned object.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)
  el <- get_elements(slide_obj, type = "text")[[1]]

  sty <- get_text_style(el)
  # sty is a text_style for uniform styling or a style_rule for mixed styling

  # Recreate the element at the same position with the extracted style:
  add_text(
    slide_obj,
    get_text(el),
    get_position(el),
    text_style = sty
  )
}

}
\seealso{
\code{\link[=get_text]{get_text()}}, \code{\link[=text_style]{text_style()}}, \code{\link[=style_rule]{style_rule()}}
}
