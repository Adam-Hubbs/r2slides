% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_generics.R
\name{get_image}
\alias{get_image}
\title{Get image URL or file path from an image element}
\usage{
get_image(x, ...)
}
\arguments{
\item{x}{An \code{image_element} object.}

\item{download}{Logical. If \code{FALSE} (default), returns the content URL
as a character string. If \code{TRUE}, downloads the image to a temp file
(\code{.png} extension) and returns the file path.}
}
\value{
A single character string: either the signed content URL
(\code{download = FALSE}) or an absolute path to a temporary file
(\code{download = TRUE}).
}
\description{
Extracts the content URL for an image element from the raw API node.
By default the URL is returned as a string. Set \code{download = TRUE} to
download the image to a temporary file and return the file path instead.

The content URL is a short-lived signed URL issued by Google; it is valid
for the duration of the current session but should not be stored permanently.

Only defined for \code{image_element} objects; calling on any other element type
errors with an informative message.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)
  el <- get_elements(slide_obj, type = "image")[[1]]

  # Get the signed URL
  url <- get_image(el)

  # Download to a temp file
  path <- get_image(el, download = TRUE)
}

}
\seealso{
\code{\link[=get_raw]{get_raw()}}, \code{\link[=add_image]{add_image()}}
}
