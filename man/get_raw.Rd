% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_generics.R
\name{get_raw}
\alias{get_raw}
\title{Get the raw API node for an element}
\usage{
get_raw(x, ...)
}
\arguments{
\item{x}{An \code{element} object or subclass.}
}
\value{
A named list representing one entry from the Google Slides API
\code{pageElements} array.
}
\description{
Fetches the raw page-element list from the Google Slides API and returns the
entry whose \code{objectId} matches \code{x@element_id}. This is the single point of
API contact for all element readers; all other getters (\code{get_position()},
\code{get_text()}, etc.) call \code{get_raw()} first and then parse the result
locally.

The slide's page data is cached with a 15-second TTL by
\code{presentation$get_page_raw()}, so repeated calls within the same session
do not necessarily trigger an HTTP request.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)
  el <- get_elements(slide_obj, type = "text")[[1]]
  raw <- get_raw(el)
  names(raw)
}

}
\seealso{
\code{\link[=get_position]{get_position()}}, \code{\link[=get_text]{get_text()}}, \code{\link[=get_text_style]{get_text_style()}}, \code{\link[=get_image]{get_image()}},
\code{\link[=get_linked_sheet]{get_linked_sheet()}}, \code{\link[=get_table]{get_table()}}
}
