% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_elements.R
\name{get_elements}
\alias{get_elements}
\title{Get elements from a slide}
\usage{
get_elements(slide, type = NULL, within = NULL, contains = c("any", "all"))
}
\arguments{
\item{slide}{A \code{slide} object (from \code{\link[=on_slide_id]{on_slide_id()}}, \code{\link[=on_slide_number]{on_slide_number()}},
etc.).}

\item{type}{Optional character vector of element types to keep. Valid values:
\code{"text"}, \code{"image"}, \code{"chart"}, \code{"table"}, \code{"shape"}, \code{"unknown"}. If
\code{NULL} (default) all element types are returned.}

\item{within}{Optional. Either a \code{slide_position} object (a rectangular
query region) or a numeric vector of length 2 \code{c(top, left)} in inches (a
single point). For a point, elements whose bounding box contains the point
are returned. If \code{NULL} (default), no spatial filtering is applied.}

\item{contains}{One of \code{"any"} (default) or \code{"all"}. Controls how an
element's bounding box is compared to a \code{slide_position} \code{within} region:
\itemize{
\item \code{"any"}: element is kept if it \emph{overlaps} the region.
\item \code{"all"}: element is kept only if it is \emph{fully inside} the region.
}

Ignored when \code{within} is a point.}
}
\value{
A list of element objects (\code{text_element}, \code{image_element}, etc.).
Returns an empty list if no elements match the filters.
}
\description{
Retrieves elements from a slide as a list of typed element objects, with
optional filtering by element type and/or spatial region.
}
\details{
\strong{Type filtering} (\code{type}): Supply one or more of \code{"text"}, \code{"image"},
\code{"chart"}, \code{"table"}, \code{"shape"}, \code{"unknown"} to restrict the result to
those element types. Uses \code{\link[=element_type]{element_type()}} for classification.

\strong{Spatial filtering} (\code{within} / \code{contains}): \code{within} can be either a
region or a single point.
\itemize{
\item A \code{slide_position} defines a rectangular region. The \code{contains} argument
then controls strictness:
\itemize{
\item \code{"any"} (default): keep elements whose bounding box \emph{intersects} the
region (any overlap).
\item \code{"all"}: keep elements whose bounding box is \emph{fully contained} within the
region (strict containment).
}
\item A length-2 numeric vector \code{c(top, left)} in inches defines a single point.
Elements whose bounding box \emph{contains that point} are returned, and
\code{contains} is ignored.
}

Rotation is accounted for when computing bounding boxes via
\code{slide_position}'s \code{bounding_box()} computed property.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)

  # All elements
  all_els <- get_elements(slide_obj)

  # Text elements only
  text_els <- get_elements(slide_obj, type = "text")

  # Elements overlapping the top-left quadrant
  region <- slide_position(top = 0, left = 0, width = 5, height = 2.8)
  top_left_els <- get_elements(slide_obj, within = region)

  # Elements fully contained within the region
  inner_els <- get_elements(slide_obj, within = region, contains = "all")

  # Elements covering the point 2" down, 3" from the left
  at_point <- get_elements(slide_obj, within = c(2, 3))
}

}
\seealso{
\code{\link[=get_all_elements]{get_all_elements()}}, \code{\link[=element_type]{element_type()}}, \code{\link[=get_raw]{get_raw()}},
\code{\link[=get_position]{get_position()}}, \code{\link[=get_text]{get_text()}}, \code{\link[=get_text_style]{get_text_style()}}
}
