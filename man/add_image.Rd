% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_image.R
\name{add_image}
\alias{add_image}
\title{Add image to a Google Slides presentation}
\usage{
add_image(
  slide_obj,
  image,
  position,
  fit = c("fill", "natural"),
  dpi = NULL,
  order = c("front", "back"),
  replacement_strategy = get_replacement_strategy(),
  match_fn = get_match_fn()
)
}
\arguments{
\item{slide_obj}{A Google Slides slide object.}

\item{image}{A ggplot object, a local file path, or a publicly accessible URL string.
If an image is specified, it must be in one of the following formats:
\itemize{
\item png
\item jpg
\item gif
}}

\item{position}{An object of class \code{r2slides::slide_position}.}

\item{fit}{Controls how the image is sized on the slide. One of:
\itemize{
\item \code{"fill"} (default): the image occupies exactly the \code{position} bounding
box, ignoring aspect ratio. For ggplot objects, the plot is rendered
directly at the target dimensions. For local files, the \code{magick} package
is required to resize the image before upload.
\item \code{"natural"}: the image is placed at its true pixel dimensions (converted
to inches using \code{dpi}), anchored to the top-left corner of \code{position}.
The image may overflow the bounding box. Requires the \code{magick} package.
}}

\item{dpi}{Numeric. Only used when \code{fit = "natural"}. The DPI to use when
converting pixels to inches. \code{NULL} auto-detects from the file metadata,
falling back to 96.}

\item{order}{Optional. One of \code{"front"} or \code{"back"}. Controls the Z-order of
the created element. Default: \code{"front"}.}

\item{replacement_strategy}{One of \code{"add"} (default), \code{"replace"}, or
\code{"skip"}. Overrides the package-wide default set by
\code{\link[=set_replacement_strategy]{set_replacement_strategy()}}. \code{"add"} always creates a new element.
\code{"replace"} deletes any matching element and creates a fresh one. \code{"skip"}
leaves a matching element untouched and suppresses creation.}

\item{match_fn}{A function that identifies an existing slide element matching
the new one. Must have signature
\verb{function(new_spec, existing_elements) -> character(1) | NULL}. See
\code{\link[=match_by_type_and_position]{match_by_type_and_position()}} for details and the built-in implementation.
Overrides the package-wide default set by \code{\link[=set_match_fn]{set_match_fn()}}.}
}
\value{
The Google Slides slide object (invisibly).
}
\description{
Inserts an image onto a slide. Accepts a ggplot object, a local file path,
or a publicly accessible URL.

When given a ggplot or local file, the image is temporarily uploaded to
Google Drive, inserted into the slide, then deleted from Drive.
The image is permanently embedded in the presentation after insertion.
}
\examples{
if(FALSE) {
plot <- ggplot(mtcars, aes(x = cyl, y = hp)) +
           geom_point()

  on_slide_number(4) |>
    add_image(plot, position = in_top_left())
}
}
