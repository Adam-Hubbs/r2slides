% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replacement_strategy.R
\name{set_replacement_strategy}
\alias{set_replacement_strategy}
\title{Set the replacement strategy}
\usage{
set_replacement_strategy(strategy = c("add", "replace", "skip"))
}
\arguments{
\item{strategy}{One of \code{"add"}, \code{"replace"}, or \code{"skip"}.}
}
\value{
The strategy string, invisibly.
}
\description{
Controls how \verb{add_*} functions behave when an existing element on the slide
matches the new one (as determined by the active match function).
}
\details{
\itemize{
\item \code{"add"} (default): always create a new element; match function is never called.
\item \code{"replace"}: delete the matched element, then create a fresh one. If no
match is found, a new element is created.
\item \code{"skip"}: leave the matched element untouched and skip creation. If no
match is found, a new element is created.
}
}
\seealso{
\code{\link[=get_replacement_strategy]{get_replacement_strategy()}}, \code{\link[=set_match_fn]{set_match_fn()}},
\code{\link[=match_by_type_and_position]{match_by_type_and_position()}}
}
