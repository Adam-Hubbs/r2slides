% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_generics.R
\name{get_table}
\alias{get_table}
\title{Get table cell text from a table element}
\usage{
get_table(x, ...)
}
\arguments{
\item{x}{A \code{table_element} object.}
}
\value{
A tibble with columns:
\itemize{
\item \code{row} (\code{integer}): 1-based row index.
\item \code{col} (\code{integer}): 1-based column index.
\item \code{text} (\code{character}): concatenated text content of the cell.
}
}
\description{
Extracts the text content from each cell of a Google Slides table element
and returns it as a tidy tibble with one row per cell. If a cell contains
multiple text runs they are concatenated before being returned.

Only defined for \code{table_element} objects; calling on any other element type
errors with an informative message.
}
\details{
The current implementation captures text only. Cell styles, borders, and
merged-cell spans are not yet extracted. Full \code{r2slides_table}
reconstruction is planned for a future release.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)
  el <- get_elements(slide_obj, type = "table")[[1]]
  tbl <- get_table(el)
  tbl
}

}
\seealso{
\code{\link[=get_raw]{get_raw()}}, \link{table_element}
}
