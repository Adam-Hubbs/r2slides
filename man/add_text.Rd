% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_text.R
\name{add_text}
\alias{add_text}
\title{Add text in a Google Slides presentation}
\usage{
add_text(
  slide_obj,
  text,
  position,
  element_id = NULL,
  text_style = NULL,
  order = c("front", "back"),
  replacement_strategy = get_replacement_strategy(),
  match_fn = get_match_fn(),
  ...
)
}
\arguments{
\item{slide_obj}{A Google Slides slide object.}

\item{text}{A character string of text to add.}

\item{position}{An object of class \code{r2slides::slide_position}}

\item{element_id}{Optional. A string ID of an existing text element to update. If element_id is \code{NULL} a new element will be created.}

\item{text_style}{Optional. A list of text styling properties. One of:
\itemize{
\item NULL (the default): Styling is determined by the defaults for the Google Slides presentation
\item r2slides::text_style object: List of Text styling
\item r2slides::style_rule object: Conditionally formatted object that defined styles and when to use those styles. Resolves to a text_style object.
}}

\item{order}{Optional. One of \code{"front"} or \code{"back"}. Controls the Z-order of the
created element. Default: \code{"front"}. Ignored when updating an existing element
via \code{element_id}.}

\item{replacement_strategy}{One of \code{"add"} (default), \code{"replace"}, or
\code{"skip"}. Overrides the package-wide default set by
\code{\link[=set_replacement_strategy]{set_replacement_strategy()}}. \code{"add"} always creates a new element.
\code{"replace"} deletes any matching element and creates a fresh one. \code{"skip"}
leaves a matching element untouched and suppresses creation.}

\item{match_fn}{A function that identifies an existing slide element matching
the new one. Must have signature
\verb{function(new_spec, existing_elements) -> character(1) | NULL}. See
\code{\link[=match_by_type_and_position]{match_by_type_and_position()}} for details and the built-in implementation.
Overrides the package-wide default set by \code{\link[=set_match_fn]{set_match_fn()}}.}

\item{...}{Additional values available to style_rule objects.}
}
\value{
The Google Slides slide object (invisibly).
}
\description{
Adds text into a new text box on a Google Slides presentation. Text can be formatted by text_style.
}
\examples{
if(FALSE) {
on_slide_number(2) |>
  add_text("Hello there!", position = in_top_left())
}
}
