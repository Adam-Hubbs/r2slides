% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_chart.R
\name{add_linked_chart}
\alias{add_linked_chart}
\title{Add a linked chart form Google Sheets}
\usage{
add_linked_chart(
  chart_obj,
  slide_obj,
  position,
  linked = TRUE,
  order = c("front", "back"),
  replacement_strategy = get_replacement_strategy(),
  match_fn = get_match_fn()
)
}
\arguments{
\item{chart_obj}{A Google Sheets chart object}

\item{slide_obj}{A Google Slides slide object}

\item{position}{An object of class \code{r2slides::slide_position}}

\item{linked}{Optional. A logical indicating whether the chart should be linked.
\itemize{
\item \code{TRUE} (the default): The chart is linked so updates made in the Google Sheet will also appear on the presentation.
\item \code{FALSE}: Inserts an image of the chart as it currently exists. Will not automatically update.
}}

\item{order}{Optional. One of \code{"front"} or \code{"back"}. Controls the Z-order of the
created chart element. Default: \code{"front"}.}

\item{replacement_strategy}{One of \code{"add"} (default), \code{"replace"}, or
\code{"skip"}. Overrides the package-wide default set by
\code{\link[=set_replacement_strategy]{set_replacement_strategy()}}. \code{"add"} always creates a new element.
\code{"replace"} deletes any matching element and creates a fresh one. \code{"skip"}
leaves a matching element untouched and suppresses creation.}

\item{match_fn}{A function that identifies an existing slide element matching
the new one. Must have signature
\verb{function(new_spec, existing_elements) -> character(1) | NULL}. See
\code{\link[=match_by_type_and_position]{match_by_type_and_position()}} for details and the built-in implementation.
Overrides the package-wide default set by \code{\link[=set_match_fn]{set_match_fn()}}.}
}
\value{
The Google Slides slide object
}
\description{
\code{add_linked_chart()} takes an existing Google Sheets chart and embeds it in a Google Slides presentation.
By default, it is \emph{linked}, so updates made to the chart will also update on the presentation.
}
\details{
\code{add_linked_chart()} puts the inputed chart in the exact area of the supplied \code{slides_position} object.
This means that your chart may be re-sized.
}
\examples{
if(FALSE) {
# Assumes that there is a chart already made on the references sheet
 chart_data |>
   write_gs("Seahawks vs Dolphins Points Per Game") |>
   get_chart_id() |>
   add_linked_chart(
     on_slide_number(
       4
     ),
     in_top_left()
  )
}
}
