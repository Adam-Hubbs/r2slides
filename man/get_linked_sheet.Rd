% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_generics.R
\name{get_linked_sheet}
\alias{get_linked_sheet}
\title{Get the linked Google Sheet chart reference from a chart element}
\usage{
get_linked_sheet(x, ...)
}
\arguments{
\item{x}{A \code{chart_element} object.}
}
\value{
A \code{chart_id} S7 object with properties \code{spreadsheet_id},
\code{sheet_id}, and \code{chart_id}.
}
\description{
Resolves the chart's \code{spreadsheetId} and \code{chartId} from the raw API node
back to a \code{chart_id} object. The function makes one API call to the Google
Sheets API to identify which sheet tab within the spreadsheet contains the
chart, so the returned \code{chart_id} carries \code{spreadsheet_id}, \code{sheet_id}, and
\code{chart_id}.

This is useful for round-tripping: extract the reference from an existing
linked chart element and pass it to \code{add_linked_chart()} to insert it on
another slide.

Only defined for \code{chart_element} objects; calling on any other element type
errors with an informative message.
}
\examples{
if (FALSE) {
  ps <- register_presentation(id = "YOUR_PRESENTATION_ID", set_active = FALSE)
  slide_obj <- on_slide_number(1, ps)
  el <- get_elements(slide_obj, type = "chart")[[1]]
  cid <- get_linked_sheet(el)
  cid@spreadsheet_id
  cid@chart_id
}

}
\seealso{
\code{\link[=get_raw]{get_raw()}}, \code{\link[=chart_id]{chart_id()}}
}
