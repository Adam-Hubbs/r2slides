% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replacement_strategy.R
\name{set_match_fn}
\alias{set_match_fn}
\title{Set the match function}
\usage{
set_match_fn(fn)
}
\arguments{
\item{fn}{A function with the signature described above.}
}
\value{
\code{fn}, invisibly.
}
\description{
Sets the package-wide function used to find an existing slide element that
matches the one being added. Used by \verb{add_*} functions when the replacement
strategy is \code{"replace"} or \code{"skip"}.
}
\details{
The function must accept two arguments:
\itemize{
\item \code{new_spec}: a named list with \code{type} (character), \code{left}, \code{top}, \code{width},
\code{height} (numeric, in inches).
\item \code{existing_elements}: a tibble with the same columns plus \code{element_id}.
}

It must return either a single \code{element_id} string or \code{NULL}. If multiple
elements match, the function should warn and return \code{NULL}.
}
\seealso{
\code{\link[=get_match_fn]{get_match_fn()}}, \code{\link[=match_by_type_and_position]{match_by_type_and_position()}}
}
