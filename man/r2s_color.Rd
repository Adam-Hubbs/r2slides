% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color.R
\name{r2s_color}
\alias{r2s_color}
\alias{solid_color}
\alias{theme_color}
\title{Color classes}
\usage{
solid_color(color, alpha = NULL)

theme_color(theme, alpha = NULL)
}
\arguments{
\item{alpha}{\code{NULL} or a number in [0, 1]. \code{NULL} (the default) omits the
alpha field from API requests, which Google Slides treats as fully opaque.
Only meaningful in fill contexts; ignored with a warning in text-style
contexts.}

\item{color}{Hex string (e.g. \code{"#4285F4"} or \code{"#F4A"}), a named R color
(e.g. \code{"red"}), or a numeric vector \code{c(r, g, b)} with each value in
[0, 1].}

\item{theme}{One of \code{"DARK1"}, \code{"LIGHT1"}, \code{"DARK2"}, \code{"LIGHT2"},
\code{"ACCENT1"}–\code{"ACCENT6"}, \code{"HYPERLINK"}, \code{"FOLLOWED_HYPERLINK"},
\code{"TEXT1"}, \code{"BACKGROUND1"}, \code{"TEXT2"}, \code{"BACKGROUND2"}.}
}
\description{
r2slides represents colors as one of two concrete classes, both of which are
subclasses of \code{r2s_color}:
}
\details{
\itemize{
\item \code{solid_color} — a color with known RGB values. Accepts a hex string,
a named R color, or a numeric \code{c(r, g, b)} vector.
\item \code{theme_color} — a reference to a named Google Slides presentation theme
color. The actual RGB value is not resolved until the slide is rendered.
}
}
